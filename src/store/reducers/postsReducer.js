const initialState = {
  // initial state properties
};

const postsReducer = (state = initialState, action) => {
  switch (action.type) {
    // handle actions
    default:
      return state;
  }
};

export default postsReducer;
