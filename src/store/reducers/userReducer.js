const initialState = {
  // initial state properties
};

const userReducer = (state = initialState, action) => {
  switch (action.type) {
    // handle actions
    default:
      return state;
  }
};

export default userReducer;
